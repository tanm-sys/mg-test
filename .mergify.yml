queue_rules:
  - name: default
    merge_conditions:
      - check-success=required-ci
pull_request_rules:
  - name: mergeme
    conditions:
      - check-success=required-ci
    actions:
      merge:
        method: merge
